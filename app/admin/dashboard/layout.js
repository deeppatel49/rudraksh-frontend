export const metadata = {
  title: "Dashboard | Rudraksh Pharmacy Admin",
  description: "Admin dashboard for Rudraksh Pharmacy management.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
