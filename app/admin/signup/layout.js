export const metadata = {
  title: "Admin Sign Up | Rudraksh Pharmacy",
  description: "Create an admin account for Rudraksh Pharmacy backend dashboard.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminSignUpLayout({ children }) {
  return children;
}
