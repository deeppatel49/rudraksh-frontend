export const metadata = {
  title: "Admin Login | Rudraksh Pharmacy",
  description: "Sign in to Rudraksh Pharmacy admin dashboard.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLoginLayout({ children }) {
  return children;
}
